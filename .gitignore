node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# .env is committed on purpose: it holds public portfolio config
# (name, links, theme) with no secrets. Local-only overrides go in
# .env.local, which stays ignored.
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
