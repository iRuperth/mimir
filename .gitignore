node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# .env is ignored — copy .env.example to .env and fill in your own
# portfolio config (name, links, theme). It holds no secrets, but each
# deploy provides its own identity, so it is not committed.
.env
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
